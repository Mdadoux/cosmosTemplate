<!DOCTYPE html>

<title>New Project</title>

{% block body %}
    <h1>Create new Project</h1>

    {{ include('admin/project/_form.html.twig') }}

    <a href="{{ path('projects') }}">back to list</a>
{% endblock %}
